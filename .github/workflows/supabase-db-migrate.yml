name: Supabase DB Migrate

on:
  push:
    branches:
      - main
      - claude/**
  workflow_dispatch:

concurrency:
  group: supabase-db-migrate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required migration secret
        run: |
          set -euo pipefail
          if [ -z "${SUPABASE_DB_PASSWORD:-}" ]; then
            echo "Missing required secret: SUPABASE_DB_PASSWORD" >&2
            exit 1
          fi

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link project
        run: |
          supabase link \
            --project-ref toitgmaeuscrdwbpntda \
            --password "$SUPABASE_DB_PASSWORD"

      - name: Push database migrations
        run: |
          supabase db push \
            --linked \
            --password "$SUPABASE_DB_PASSWORD"
