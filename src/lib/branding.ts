/**
 * Centralized branding constants.
 * Update these values to rebrand the application.
 */

/** The short product / company name shown in headers, login, and driver UI. */
export const APP_NAME = "RugBoost";

/** Tagline shown on the internal dashboard home page. */
export const APP_TAGLINE = "Mission Control";

/** Label used in the driver-facing portal header. */
export const DRIVER_TITLE = `${APP_NAME} Driver`;
